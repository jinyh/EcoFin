# docs 目录说明

`docs/` 只放项目维护文档，不放对外正文内容。

## 分类

- `architecture/`：架构决策记录（ADR）
- `planning/`：中长期规划、扩充路线、质量标准
- `tracking/`：可持续更新的进度与任务跟踪
- `reports/`：带日期的阶段总结、复盘与审查报告

## 边界

- 对外知识正文放在 `book/`
- 版本归档放在 `archive/`
- 外部来源快照放在 `sources/`
